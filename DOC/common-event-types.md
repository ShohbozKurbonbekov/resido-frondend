## React.ChangeEvent<HTMLInputElement> → inputs, selects, textareas

## React.MouseEvent<HTMLButtonElement> → clicks on buttons, divs, etc.

## React.FormEvent<HTMLFormElement> → form submissions

## React.KeyboardEvent<HTMLInputElement> → key presses

## React.FocusEvent<HTMLInputElement> → focus/blur

## SyntheticEvent → only when you don’t care about the element type (like in your Tabs example)
