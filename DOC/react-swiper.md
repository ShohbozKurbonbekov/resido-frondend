### Autoplay: Automatically changes slides after some time.

### Pagination: Adds clickable bullets to navigate between slides.

### Navigation: Adds next/prev arrows.

### useRef() is used to store references to the progress circle SVG and text content.

We'll use these to update the progress display of the autoplay.
