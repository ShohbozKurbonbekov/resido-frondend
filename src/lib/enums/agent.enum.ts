export enum MemberType {
  USER = "USER",
  AGENT = "AGENT",
  AGENCY = "AGENCY",
  REAL_ESTATE_ADMIN = "REAL_ESTATE_ADMIN",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCKED = "BLOCK",
  DELETED = "DELETE",
}
export enum AgentStatus {
  PENDING = "pending",
  AVAILABLE = "available",
  PAUSED = "paused",
}
